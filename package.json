{
  "name": "react-sample",
  "version": "1.0.0",
  "description": "this is a project for walking throught the documents of React",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "babel src --out-dir dist --watch",
    "buildreact": "react-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "react-scripts": "^1.0.14"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
