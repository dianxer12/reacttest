import {MenuActionType} from '../util'

const initialState = {
    charts: []
}

const dashCharts = (state=initialState,action) => {
    switch(action.type){
        default:
            return state
    }
}